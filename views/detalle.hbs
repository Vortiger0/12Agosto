<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{titulo}}</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body>
    {{> titulo }}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card shadow">
                    <div class="card-body text-center">
                        <img src="{{juego.imagen}}" alt="" class="img-fluid rounded mb-4" style="max-height: 250px;">
                        <h2 class="mb-3">{{juego.nombre}}</h2> 
                        <h4 class="text-primary mb-3">{{juego.precio}}$</h4>
                        <p class="mb-4">{{juego.descripcion}}</p>
                        <a href="/" class="btn btn-info">Volver</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>