<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{titulo}}</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    {{> titulo }}
    <form action="{{action}}" method="POST">
        <input type="text" name="nombre" placeholder="Nombre" value="{{juego.nombre}}">
        <input type="text" name="precio" placeholder="Precio" value="{{juego.precio}}">
        <input type="text" name="descripcion" placeholder="Descripción" value="{{juego.descripcion}}">
        <input type="text" name="imagen" placeholder="Nombre de imagen" value="{{juego.imagen}}">
        <button type="submit">{{boton}}</button>
    </form>
  
</body>
</html>